import { Section } from "@/components";

export default function Home() {

    return (
        <Section className="w-screen flex items-center justify-center">
            <span className="text-7xl text-white">NEW TOKEN</span>
        </Section>
    )
}